// Js is Dynamic typeing type
const sum = (x, y) => {
    return x + y;
}
result1 = sum(1, 2)
console.log("Check sum with argument two number = " + result1 + " and type of result1 is: " + typeof result1);

const sum2 = (x, y) => x + y;
result2 = sum2("AGE: ", 22)
console.log("Check sum with argument string and number = " + result2 + " and type of result2 is: " + typeof result2);
